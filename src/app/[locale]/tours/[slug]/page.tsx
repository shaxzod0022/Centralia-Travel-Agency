import { SignatureJourneys } from "@/components";
import React from "react";

const page = () => {
  return (
    <div className={`mt-20`}>
      <SignatureJourneys />
    </div>
  );
};

export default page;
